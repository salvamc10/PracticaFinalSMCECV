<!-- night/themes.xml -->
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.PracticaFinalSMCECV" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@android:color/white</item>
        <!-- Otros atributos de estilo... -->
    </style>

    <!-- Estilo específico para los botones en modo oscuro -->
    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <!-- Ajusta cualquier otro atributo específico para el modo oscuro -->
        <item name="android:background">@drawable/button_rounded_type2</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">15dp</item>
    </style>
</resources>
